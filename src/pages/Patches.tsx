import { Package } from 'lucide-react'
import { ComingSoon } from '@/components/ComingSoon'

export default function Patches() {
  return (
    <ComingSoon
      title="Patch Management"
      description="Kelola semua patch yang sudah dirilis"
      icon={Package}
    />
  )
}
